services:
  - type: web
    name: coin-yab-bot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: TELEGRAM_TOKEN
        sync: false
      - key: ALPHA_VANTAGE_KEY
        sync: false
