services:
  - type: web
    name: coin-yab-bot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: ALPHA_VANTAGE_KEY
        sync: false
